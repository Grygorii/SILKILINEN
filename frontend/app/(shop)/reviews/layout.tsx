export const metadata = {
  title: 'Customer Reviews — SILKILINEN',
  description: 'Read verified customer reviews for SILKILINEN silk and linen intimates. Real opinions from real buyers.',
};

export default function ReviewsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
